#!/usr/bin/env bun
import { main } from "../src/lib/orchestrator";

const run = async () => {
  try {
    await main();
  } catch (error) {
    console.error("Orchestrator failed", error);
    process.exitCode = 1;
  }
};

run();
